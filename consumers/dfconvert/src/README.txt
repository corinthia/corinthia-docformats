This file is only present because git does not store empty directories.

The directory will eventually contain the dfconvert program, which will consists of the code currently in dfutil that handles command-line conversion of files between different formats.
