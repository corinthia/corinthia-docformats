This file is only present because git does not store empty directories.

The directory will eventually "black box" tests for the DocFormats library, i.e. those which are run using a command-line tool invoked by a script, rather than testing the API itself.
